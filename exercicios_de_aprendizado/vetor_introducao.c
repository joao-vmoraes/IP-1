//um veto recebe seu tipo(int,char,float), seu indentificado(nome) e sua quantidade[quantidade]  ex: int numeros[10], o vetor numeros recebe 10 valores.

int main(void){
    float reais[5];

    reais[0] = 3.14; // a primeira posição da memoria do vetor é o 0 e a ultima é 4, logo, 5 elementos no total,e aqui estou atribuindo a posição 0 = 3.14
    reais[4] = 1.41;

    int inteiros[5] = {1 , 2 , 3 , 4 , 5 }; //declaração automatica e simples de vetores.
}